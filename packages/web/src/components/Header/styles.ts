import { styled } from '@stitches/react'

export const SearchContainer = styled('div', {
  borderRadius: '3px',
  display: 'flex',
  alignItems: 'center',
  background:
    'linear-gradient(269.39deg, rgba(255, 255, 255, 0.25) 0%, rgba(255, 255, 255, 0.15) 100%)',
  backgroundBlendMode: 'overlay',
  boxShadow: '0px 0px 79px 3px rgba(0, 0, 0, 0.05)',
  backdropFilter: 'blur(30px)',
})
